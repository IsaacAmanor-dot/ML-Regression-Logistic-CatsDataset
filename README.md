# ML-Regression-Logistic-CatsDataset
This project implements gradient descent, linear regression, and logistic regression on synthetic and image datasets. It analyzes convergence, model performance, and overfitting, including cat vs. non-cat classification with both original and modified train–test splits.
